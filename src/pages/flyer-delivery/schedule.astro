---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { loadPage } from '../../lib/content.ts';
import ContentBlock from '../_partials/ContentBlock.astro';
import SectionHeader from '../../components/SectionHeader.astro';

const slug =
  'flyer-delivery-services-perth-https-freakyflyerdelivery-com-au-copy-of-ffd-delivery-schedule-8';
const page = await loadPage(slug);
const fallbackTitle = 'Delivery Schedule';
const fallbackDescription =
  'Upcoming flyer delivery runs across Perth so you can plan campaigns.';
const frontmatter = page?.fm ?? {};
const rawTitle = typeof frontmatter.title === 'string' ? frontmatter.title : '';
const rawDescription =
  typeof frontmatter.description === 'string' ? frontmatter.description : '';
const title = rawTitle.trim().length > 0 ? rawTitle : fallbackTitle;
const description =
  rawDescription.trim().length > 0 ? rawDescription : fallbackDescription;
---

<BaseLayout {title} description={description}>
  {
    page ? (
      <>
        <section class="section surface">
          <div class="container">
            <SectionHeader title={page.fm?.title ?? 'Delivery Schedule'} />
          </div>
        </section>
        <ContentBlock html={page.html} />
      </>
    ) : (
      <section class="section surface">
        <div class="container">
          <SectionHeader title="Delivery Schedule" />
          <p>
            Check the latest run dates across Perth so you can align your flyer
            production and campaign launches.
          </p>
        </div>
      </section>
    )
  }
</BaseLayout>
