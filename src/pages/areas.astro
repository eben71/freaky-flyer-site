---
import BaseLayout from '../layouts/BaseLayout.astro';
import { loadPage } from '../lib/content.ts';
import ContentBlock from './_partials/ContentBlock.astro';

const slug = 'areas';
const page = await loadPage(slug);
const fallbackTitle = 'Service Areas | Freaky Flyer Delivery';
const fallbackDescription = '';
const frontmatter = page?.fm ?? {};
const rawTitle = typeof frontmatter.title === 'string' ? frontmatter.title : '';
const rawDescription = typeof frontmatter.description === 'string' ? frontmatter.description : '';
const title = rawTitle.trim().length > 0 ? rawTitle : fallbackTitle;
const description = rawDescription.trim().length > 0 ? rawDescription : fallbackDescription;
---
<BaseLayout {title} description={description}>
  {page ? (
    <ContentBlock html={page.html} />
  ) : (
    <>
      <section class="section">
        <div class="container">
          <h1>Service Areas</h1>
          <p>
            We specialise in Perth metro deliveries, covering northern and southern corridors with walkers matched to the suburbs
            you need to reach most.
          </p>
        </div>
      </section>

      <section class="section surface" aria-labelledby="areas-legend">
        <div class="container">
          <h2 id="areas-legend">Coverage legend</h2>
          <div class="card">
            <p>Interactive maps are on the way. For now, use this legend to brief our team on where to focus.</p>
            <ul>
              <li><strong>Core suburbs:</strong> Regular weekly drops with walkers familiar to the streets.</li>
              <li><strong>Expansion zones:</strong> Scheduled fortnightly with lead time for bundling and transport.</li>
              <li><strong>Campaign hotspots:</strong> High-priority runs planned alongside in-store or event activations.</li>
            </ul>
          </div>
        </div>
      </section>
    </>
  )}
</BaseLayout>
